{
  "name": "protogaea-viewer",
  "private": true,
  "version": "0.0.1",
  "description": "The Protogaea viewer: the map, live mode and permanent links (stage B2)",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "wasm": "cargo build --release -p protogaea-wasm --target wasm32-unknown-unknown && node scripts/copy-wasm.mjs",
    "build": "npm run wasm && tsc --noEmit && vite build",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@fontsource-variable/geist": "^5.3.0",
    "@fontsource-variable/geist-mono": "^5.3.0",
    "@phosphor-icons/web": "^2.1.2",
    "pixi.js": "^8.21.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.1"
  }
}
